---
title: "Seite Nicht Verfügbar"
layout: gridlay
sitemap: true
permalink: /de/404.html
---

<div class="section-card" style="text-align: center; padding: var(--space-16) var(--space-8);" markdown="0">
<h1 style="font-size: 5rem; margin-bottom: var(--space-2); color: var(--accent); font-family: 'DM Sans', sans-serif;">404</h1>
<p style="font-size: 1.125rem; margin-bottom: var(--space-8); text-align: center; margin-left: auto; margin-right: auto; max-width: 600px;">Die gesuchte Seite ist leider nicht verügbar .<br>Stattdessen ist hier einer meiner liebsten XKCDs</p>


<!-- Random XKCD Image Container -->
<div id="xkcd-container" style="margin-top: var(--space-10); margin-bottom: var(--space-8); display: flex; justify-content: center;">
  <img id="xkcd-image" src="" alt="Random XKCD Comic" style="max-width: 70%; max-height: 600px; border-radius: var(--radius-lg);">
</div>

<br>

<div style="display: flex; gap: var(--space-4); justify-content: center; flex-wrap: wrap;">
<a href="{{ site.url }}{{ site.baseurl }}/" class="btn-pill btn-doi" style="padding: 0.5rem 1.5rem; font-size: 0.875rem;">Startseite</a>
<button onclick="document.getElementById('searchOverlay').classList.add('open'); document.getElementById('searchInput').focus();" class="btn-pill btn-bib" style="padding: 0.5rem 1.5rem; font-size: 0.875rem;">Suchen</button>
</div>


<div style="margin-top: var(--space-10); text-align: center;">
<h4 style="font-size: 0.875rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-muted); margin-bottom: var(--space-4);">Weitere Seiten</h4>
<div style="display: flex; gap: var(--space-8); justify-content: center; flex-wrap: wrap;">
  {% for nav in site.nav_pages %}
  <a href="{{ site.url }}{{ site.baseurl }}/{{ nav.name }}" style="color: var(--text-secondary); font-size: 0.9375rem;">{{ nav.name | capitalize }}</a>
  {% endfor %}
</div>
</div>





<script>
// Generate array from Jekyll data
const xkcdImages = [
  {% for item in site.data.xkcd %}
    "{{ item.image }}"{% unless forloop.last %},{% endunless %}
  {% endfor %}
];

// Get random image
function getRandomXKCD() {
  const randomIndex = Math.floor(Math.random() * xkcdImages.length);
  return xkcdImages[randomIndex];
}

// Set image on page load
document.addEventListener('DOMContentLoaded', function() {
  const randomImage = getRandomXKCD();
  const baseUrl = "{{ site.url }}{{ site.baseurl }}";
  const imageUrl = baseUrl + "/XKCD404/" + randomImage;
  document.getElementById('xkcd-image').src = imageUrl;
});
</script>







